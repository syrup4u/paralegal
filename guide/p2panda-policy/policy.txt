Scope:
Somewhere

Policy:
1. For each "user_data" type marked user_data:
    A. There is a "retrieval" that produces "user_data" where:
       a. There is a "sign" marked sign where:
          i) "retrieval" goes to "sign"
       and
       b. There is a "send" marked send where:
          i) "retrieval" goes to "send"
